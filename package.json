{
  "name": "root",
  "private": true,
  "workspaces": [
    "products",     
    "wishlist",
    "packages/common"
  ],  
  "scripts": {
    "dev": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run --parallel start",
    "heroku-postbuild": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  }
}
