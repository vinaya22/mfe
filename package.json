{
  "name": "root",
  "private": true,
  "workspaces": [
    "products",
    "wishlist",
    "packages/common"
  ],
  "scripts": {
    "dev": "lerna run --parallel start",
    "build": "lerna run build",
    "start": "node server.js",
    "heroku-postbuild": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
