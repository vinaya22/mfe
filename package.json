{
  "name": "root",
  "private": true,
  "workspaces": [
    "products",
    "wishlist",
    "packages/common",
    "packages/store"
  ],
  "scripts": {
    "dev": "lerna run --parallel dev",
    "build": "lerna run build",
    "start": "node index.js",
    "heroku-postbuild": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
