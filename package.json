{
  "name": "root",
  "private": true,
  "workspaces": [
    "products",     
    "wishlist",
    "packages/common"
  ],  
  "scripts": {
    "dev": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run --parallel start"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  }
}
